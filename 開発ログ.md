# 開発ログ

## 2026-04-03: プロシージャル林成長シミュレーションシーン実装
- **目的**: PineTreeGeneratorを使い、林齢10～100年の成長をリアルタイムプロシージャルで可視化
- **新規ファイル**:
  - `Assets/Scripts/Procedural/ProceduralTreeInstance.cs` — 個体データクラス（seed, growthFactor, 位置）
  - `Assets/Scripts/Procedural/TreeParameterMapper.cs` — GrowthData→PineTreeGeneratorパラメータ変換
  - `Assets/Scripts/Procedural/ProceduralForestManager.cs` — 森林全体管理（配置・段階的更新・間伐・復活）
  - `Assets/Scripts/Procedural/ProceduralForestUI.cs` — UI制御（AutoPlay/Next/Prev/Reset＋テキスト表示）
  - `Assets/Scripts/Procedural/ThinningAnimator.cs` — 間伐フェードアウト演出（Yスケール縮小）
  - `Assets/Scripts/Procedural/Editor/ProceduralForestSceneBuilder.cs` — シーン自動構築Editorスクリプト
- **設計ポイント**:
  - 個体ごとにseedベースの一貫した成長係数(0.7～1.3)を持ち、サイズ系パラメータに乗算
  - 間伐時はgrowthFactor昇順（小さい木優先）で伐採、Prev操作時は逆順で復活
  - パフォーマンス対策: 300本表示（3000本/haの1/10）、1フレーム5本ずつ段階的Generate()
  - 既存GrowthDatabase/PoissonDiskSampling/CameraControllerを再利用
- **シーン構築**: Unityメニュー `WoodSimulator > Create Procedural Forest Scene` で自動生成
- **動作確認結果**: シーン生成・Play実行ともに成功。300本の木が配置され成長UIも動作。カメラがやや遠い以外は問題なし
- **確認用ログ**: ProceduralForestManager.csにデバッグログ仕込み済み（Start/SetStage/UpdateTreesGradually/間伐）。動作確認後に削除すること
- **MCP設定修正**: `~/.claude/settings.json` に `unity-natural-mcp` のHTTP接続設定を追加（`http://localhost:56782/mcp/`）。ポートはUnity起動ごとに変わる可能性あり。Claude Code再起動で反映される
- **CameraController廃止**: ランタイムでカメラ位置を上書きしていたCameraControllerを削除。カメラ位置はシーンエディタで直接調整する方式に変更
- **パフォーマンス改善**:
  - ProceduralForestManager: 固定本数/フレーム方式からフレーム時間バジェット方式（デフォルト8ms）に変更。フレームレートに応じて自動的に処理量を調整
  - PineTreePreset.Generate(): ランタイム時に子GameObjectを破棄せずメッシュだけ差し替えるよう最適化（GC負荷・生成コスト削減）
- **デバッグログ削除**: ProceduralForestManager.csの確認用Debug.Logをすべて削除（Debug.LogErrorのエラー通知のみ残存）
- **自動進行の更新待ち修正**: AutoPlayCoroutineがモデル更新完了（IsUpdating==false）を待たずに次のステージへ進んでいた問題を修正。更新完了後にインターバルを開始するよう変更
- **メッシュ生成の大幅最適化**:
  - ランタイム専用ビルダー追加（葉メッシュ共有化、Meshオブジェクト再利用、配列プリアロケート、BranchPlacements共有）
  - runtimeQualityパラメータ（デフォルト0.5）で頂点数・枝本数・葉枚数を動的削減
  - フレームバジェットを16msに引き上げ
  - Converting invalid MinMaxAABB警告を修正（CombineMeshes前後のIsMeshFiniteチェック追加）
- **CombineMeshes完全排除・直接バッファ書き込み方式**:
  - 枝メッシュ: 数百個の中間Mesh生成+CombineMeshes → WriteBranchVertsで1パス直接書き込み（中間Mesh割り当てゼロ）
  - 葉メッシュ: 数千個のCombineInstance+CombineMeshes → WriteLeafQuadで1パス直接書き込み（CombineInstance配列廃止）
  - Mathf.Pow(sin, 非整数)の浮動小数点誤差によるNaN発生を修正（Mathf.Max(0f, ...)で負値防止）
- **その他効率化**:
  - GetActiveTrees()のリスト毎回生成 → activeTreesCacheで使い回し
  - OnUpdateProgressをフレームyield時のみ発火（毎木→フレーム境界のみ）
  - ThinningAnimatorを初期化時にアタッチしPlay/Stopで再利用（AddComponent/Destroy廃止）
  - ランタイム時のIsMeshFinite全頂点走査を省略
  - GameObject名のstring interpolation廃止
  - transform.Find文字列検索 → MeshFilter/MeshRenderer参照キャッシュ（初回のみ検索）
  - WriteBranchVerts: mat.MultiplyVector(Vector3.right)をループ外に移動、sin/cosテーブル事前計算
  - BuildTrunkMeshInto: Cos/Sinの重複計算排除、noiseフラグをループ外に
  - WriteLeafQuad: UV値をstatic readonly定数に
  - PoissonDiskSampling: 不要なDebug.Log削除
  - GetActiveTreeCount(): 呼び出し元なしのデッドコード削除
- **下枝高さの年齢連動**: branchStartHeightを年齢に応じて非線形に変化させ、成長とともに下枝のない幹部分が伸びるよう実装。20年まではほぼ変化なし、25年以降から二乗カーブで上昇し80年以降は幹の50%が枝なしに
- **個体ごとの太さ差異強化**: growthFactorの太さ系パラメータへの適用を三乗に変更。成長の良い木はより太く、成長の悪い木はより細くなり個体差が明確に
- **頂部形状の改善**: 頂部枝をroll角で下向きに傾斜させ、枝本数を倍増・葉数を増加・枝長を延長し、杉らしい円錐形の樹冠に近づけた
- **全体の葉量増加**: baseLeavesPerBranchを20-48 → 32-64に増量
- **LODプレハブ一括Bakeシステム実装**:
  - `LODTreePrefabBaker`エディタツール（メニュー: WoodSimulator > Bake LOD Tree Prefabs）
  - 代表6年齢パターン（10,25,40,55,75,100年）でLODGroup付きプレハブを自動生成
  - LOD0: フルメッシュ（3サブメッシュ）、LOD1: 簡略化メッシュ（枝・葉半減）、LOD2: ビルボードクワッド
  - GPU Instancing対応マテリアル自動有効化
  - UInt32インデックスフォーマット対応（65535頂点超過対策）
  - 出力先: Assets/BakedTrees/
- **Terrain地形システム実装**:
  - TerrainTestScene作成（500m×500m、フライカメラ付き）
  - Terrain MCPカスタムツール追加（TerrainTool.cs / TerrainTextureTool.cs）:
    - Terrain_GetInfo / Terrain_SetPerlinHeight / Terrain_CarveRiver / Terrain_Smooth / Terrain_FlattenArea
    - Terrain_AutoPaint（高さ・傾斜ベースの自動テクスチャ塗り分け: 草/森林/岩/土）
    - Terrain_AddGrass（傾斜・高さベースの草自動生成）
  - 杉林の典型的地形を反映（谷筋・沢筋・中傾斜の山地）、本流+支流の川床を彫刻
- **Terrain上への成長シミュレーション統合**:
  - ProceduralForestManagerにTerrain対応追加（SampleHeightでY座標を地形に合わせる）
  - 川底・低地除外機能（minTerrainHeight以下には配置しない）
  - TerrainTestSceneBuilder に「Add Forest To Current Scene」メニュー追加（既存シーンに成長UI・FPS付きで一括追加）
  - テレインサイズを150m×150m・高さ40mに調整（杉のスケールに適合）
  - 1000本配置で動作確認済み（パフォーマンス良好）
- **カメラ操作UI追加**: 右下にボタンパネル（前後左右・上下移動・回転）を配置。ボタン押し続けで連続操作
- **若木の葉量大幅増加**: leafCardサイズ拡大・whorlCount/branchesPerWhorl/baseLeavesPerBranch下限引き上げ。若木でもびっしり葉が茂る見た目に改善
- **ProceduralForestSceneへのLOD適用**:
  - 各木にLODGroup（LOD0: フルメッシュ / LOD1: 簡略メッシュ / カリング）を動的設定
  - LOD1用PineTreeGeneratorを子オブジェクトに配置し、枝・葉半減で同時生成
  - 樹高ベースでLODGroupサイズを手動設定（成長に応じて動的更新）
  - GameObject名を"CedarTree"に変更（Unityビルトイン"Tree"との衝突回避）
- **LOD大量配置テストシーン実装**:
  - `LODForestSceneBuilder`エディタツール（メニュー: WoodSimulator > Create LOD Forest Test Scene）
  - 200m×200mエリアに1000本のLODプレハブをPoisson配置（6年齢パターン・老齢寄り分布・個体差スケール）
  - FPSカウンター、WASD+マウスのフライカメラ（SimpleFlyCamera）付き
  - LOD切り替え・GPU Instancingの動作確認済み

## 2026-04-03: 枝メッシュのUV座標欠落バグ修正
- **対象**: `Assets/SmartCreatorProceduralTrees/Core/PineTreePreset.cs` の `BuildProceduralBranchMesh()`
- **問題**: 枝メッシュにUV座標が設定されておらず、barkMaterialのテクスチャ（BaseMap/BumpMap/OcclusionMap）がマッピングされずのっぺりした単色表示になっていた
- **原因**: `BuildTrunkMesh()`にはUV生成があったが、`BuildProceduralBranchMesh()`にはUVリストの生成・設定が欠落していた
- **修正内容**: 幹メッシュと同じ方式でUV座標を追加（U=周方向, V=長さ方向）、`mesh.SetUVs(0, uvs)` を追加
- **確認結果**: 枝に樹皮テクスチャが正常に反映されることを確認
- **補足**: Branchesが透明に見える問題は、プレハブ上ではメッシュが保存されない仕様（`[ExecuteAlways]`で動的生成）によるもので、シーン配置時にGenerate()が走れば正常に表示される
- **補足2**: サブモジュール（TozawaMCP）がクローン時に取得されていなかったため `git submodule update --init --recursive` で復元済み
